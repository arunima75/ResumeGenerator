import React from "react";

// Used to pass data and functions down the component tree without having to explicitly pass them through props at each level.
const AppContext = React.createContext();

export default AppContext;